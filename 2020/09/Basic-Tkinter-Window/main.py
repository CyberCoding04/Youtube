from tkinter import *
#Python3 - from 'tkinter'
#Python2 - from 'Tkinter'

window = Tk() # initialising a window
window.title("Second Window") # Setting the title of the window

window.geometry('{}x{}'.format(500,500))

window.mainloop()
